export enum CardBottomBorderColors {
  Red = '#eb5757',
  Green = '#27ae60',
  Blue = '#2f80ed',
  Yellow = '#f2c94c',
}

export const DAY_IN_MS = 1000 * 60 * 60 * 24;

export enum DateSortOrder {
  oldToNew = 'oldToNew',
  newToOld = 'newToOld',
  default = '',
}

export enum ViewsSortOrder {
  ascending = 'ascending',
  descending = 'descending',
  default = '',
}

export enum YTApiRoutes {
  search = '/search',
  videos = '/videos',
}
